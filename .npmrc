# Enforce Node.js version specified in package.json (engines field)
engine-strict=true

# Enable lock files for deterministic builds (consider setting to true unless explicitly required)
package-lock=false

# Install dependencies with exact versions for consistency
save-exact=true
